from monopoly import run
import argparse


if __name__=='__main__':
        #parser = argparse.ArgumentParser(description='flags for monopoly sim')
        #parser.add_argument('-w', '--watch', )
        #args = parser.parse_args()
        run()
